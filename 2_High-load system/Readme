Вы разрабатываете систему управления задачами, которая выполняет следующие действия:
M — запускает задачу
R — перезапускает задачу
C — отменяет задачу
D — завершает задачу

Процесс обработки задачи задается строкой из символов M, R, C, D.

Условия корректного процесса обработки:

Процесс должен начинаться с запуска задачи (M).
После запуска (M) задача может быть: перезапущена (R), отменена (C), завершена (D).
Перезапущенная задача (R) автоматически переводится в состояние отмененной.
Отмененная задача (C) может быть запущена заново (M).
Завершенная задача (D) может быть запущена заново (M).
Процесс обработки задачи должен заканчиваться завершением (D).
Формат входных данных:

Первая строка содержит целое число t (1 ≤ t ≤ 10⁵) — количество тестов.
Каждая из следующих t строк содержит строку s (1 ≤ |s| ≤ 10⁵), состоящую из символов M, R, C, D.
Суммарная длина всех строк не превышает 10⁵.

Формат выходных данных:
Для каждого теста выведите в отдельной строке:
YES, если процесс обработки задачи корректен.
NO, если процесс обработки задачи некорректен.

Пример ввода:
5
MRCMD
MDD
M
MDMRCMD
MMDD

Пример вывода:
YES
NO
NO
YES
NO



